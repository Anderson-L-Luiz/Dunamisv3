# Dunamis v3 dependencies (installed automatically by the launcher into .venv)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
gemini_webapi>=1.0.0
curl_cffi>=0.7.0        # real Chrome TLS/HTTP2 fingerprint (network stealth)
playwright>=1.42.0      # one-time Google login window only
requests>=2.31.0        # for the optional CLI chat (chat.py)
